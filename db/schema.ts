import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core';
export const leads = sqliteTable('leads', {id:text('id').primaryKey(),kind:text('kind').notNull(),email:text('email').notNull(),name:text('name').notNull().default(''),timezone:text('timezone').notNull().default(''),availability:text('availability').notNull().default(''),message:text('message').notNull().default(''),course:text('course').notNull(),dimension:text('dimension'),scores:text('scores'),utm:text('utm').notNull().default('{}'),consentVersion:text('consent_version').notNull(),createdAt:integer('created_at').notNull(),emailStatus:text('email_status').notNull().default('pending')},t=>[index('idx_leads_email_created').on(t.email,t.createdAt)]);
